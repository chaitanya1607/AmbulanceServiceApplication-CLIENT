import { DriverUpdateModel } from './driver-update-model';

describe('DriverUpdateModel', () => {
  it('should create an instance', () => {
    expect(new DriverUpdateModel()).toBeTruthy();
  });
});
